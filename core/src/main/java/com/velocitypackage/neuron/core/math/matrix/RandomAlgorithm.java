package com.velocitypackage.neuron.core.math.matrix;

public interface RandomAlgorithm
{
    void randomize(float[] input);

    void randomize(float[][] input);

    void randomize(float[][][] input);
}
